# Copy to .env in this repo (memento root). Postgres is deployed separately by the operator.
# DATABASE_URL is loaded via python-dotenv from {MEMENTO_ENV_ROOT}/.env when the MCP server starts.
# MEMENTO_ENV_ROOT and MEMENTO_WORKSPACE_ROOT are set by the MCP client (e.g. Cursor mcp.json).
DATABASE_URL=
